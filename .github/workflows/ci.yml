name: Secure App CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Check current directory
      run: pwd  # prints the directory the workflow is running from

    - name: List repo files
      run: ls -R  # optional, helps debug file paths

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        pip install -r app/requirements.txt

    - name: Run Bandit (Python security linter)
      run: |
        pip install bandit
        bandit -r app || true  # warnings don't fail the build

    - name: Build Docker image
      run: |
        docker build -t secure-flask-app .

    - name: Scan Docker image with Trivy
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'secure-flask-app'
        format: 'table'
        exit-code: '0'

